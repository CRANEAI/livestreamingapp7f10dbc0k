/* 
 * Contact: team@fusioncode.ai
 *
 *
 * Project  - feed UNIT TESTS
 * Application Client
 * Live Streaming App
*/

/* the exploration view has a close icon located at the very-top right of the view which on click will trigger the close() action */
it("the exploration view has a close icon located at the very-top right of the view which on click will trigger the close() action", () => {
  /* ToDo developer needs to complete unit test 
               the exploration view has a close icon located at the very-top right of the view which on click will trigger the close() action
               uncomment below and update the code to test
                //instance.action(function(error) {
                //  if (error) throw error;
                //expect().to.be();
                //});
                done();
               */
})
